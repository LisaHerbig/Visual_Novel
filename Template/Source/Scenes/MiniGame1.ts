namespace TheSearch {

    //function miniGameCatch(): void {
        //console.log("Bla");
        
   // }
    
    /*export async function game1_eggs (): Promise<boolean> {
        
        dataForSave.count += 1;

            
            //let random: number = getRandom(2, 30);
            
        if (dataForSave.count >= dataForSave.random) {
                console.log("Rex is here!");
                dataForSave.count = 0;
                let scene = document.querySelector("scene");
                scene.removeEventListener("pointerdown", game1_eggs);
                
                return dataForSave.bool = true;
            }
        return dataForSave.bool = false;
        }*/
        

}